; Example BIND zone template
$TTL 3600
@   IN SOA ns1.${DOMAIN}. hostmaster.${DOMAIN}. (
        2025102601 ; serial
        3600       ; refresh
        900        ; retry
        1209600    ; expire
        300 )      ; minimum
    IN NS ns1.${DOMAIN}.
    IN NS ns2.${DOMAIN}.
@   IN A ${IPV4}
www IN CNAME @
